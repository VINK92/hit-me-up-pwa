import React from 'react';

import { StyledContainer } from './styled';

const GradientContainer: React.FC = ({ children }) => {
  return <StyledContainer>{children}</StyledContainer>;
};

export default GradientContainer;
